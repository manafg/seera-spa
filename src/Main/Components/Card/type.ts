type CardProps = {
  /**
   * describe the name of the hotel in the card
   */
  name: string;
  /**
   * describe the price for the hotel in the card
   */
  price: string;
  /**
   * describe the city that hotel in
   */
  city: string;
};

export type { CardProps };
